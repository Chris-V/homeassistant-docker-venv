#!/usr/bin/with-contenv bashio

USER="homeassistant"
VENV="${VENV:-/var/tmp/homeassistant-venv}"

bashio::log.info "Initializing venv in $VENV"
su "$USER" \
  -c "python3 -m venv --system-site-packages '$VENV_PATH'"
